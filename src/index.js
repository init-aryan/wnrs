import React from "react";
import ReactDOM from "react-dom";
import App from "./ThemeApp";

ReactDOM.render(
  <React.StrictMode>
    <ThemeApp />
  </React.StrictMode>,
  document.getElementById("root")
);
